# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/

# OS / editor
.DS_Store
.idea/
.vscode/

# Streamlit
.streamlit/secrets.toml

# Claude Code local settings
.claude/settings.local.json

# NOTE: data/ and models/ are intentionally COMMITTED so reviewers can run the
# demo (`streamlit run app.py`) immediately without regenerating or retraining.
# The trained models total ~35 MB (largest file 15 MB < GitHub's 100 MB limit).
